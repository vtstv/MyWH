package com.murr.mywh.viewmodels

// ProductViewModel больше не используется, так как продукты объединены с папками
// Класс оставлен для совместимости

class ProductViewModel(application: android.app.Application) : androidx.lifecycle.AndroidViewModel(application) {
    // Empty - functionality moved to FolderViewModel
}

